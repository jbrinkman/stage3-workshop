# Promptfoo outputs
# Ignore the transient top-level output a bare `task eval` drops in the CWD...
/results.html
/results.json
/results.csv
*.log

# ...but DO track the per-step results captured under results/ (certification
# evidence: shows measurable improvement across Step-N tags).
!results/

# Node modules
node_modules/

# OS files
.DS_Store

# Claude Code — ignore ephemeral/machine-local files;
# keep project config (settings.json, hooks/, commands/, skills/) committable
.claude/settings.local.json
.claude/sessions/
.claude/cache/
.claude/history.jsonl
.claude/backups/
.claude/plans/
.claude/shell-snapshots/
.claude/session-env/
.claude/downloads/