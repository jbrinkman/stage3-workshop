# Promptfoo outputs — transient run artifacts (evidence lives in ITERATION_LOG.md)
*results.html
*results.json
*results.csv
*.log

# Scratch dir used by `task metrics`
tmp/

# Node modules
node_modules/

# OS files
.DS_Store

# Kiro CLI — local/machine project settings (tool-search config, etc.)
/.kiro/

# Claude Code — ignore ephemeral/machine-local files;
# keep project config (settings.json, hooks/, commands/, skills/) committable
.claude/settings.local.json
.claude/sessions/
.claude/cache/
.claude/history.jsonl
.claude/backups/
.claude/plans/
.claude/shell-snapshots/
.claude/session-env/
.claude/downloads/